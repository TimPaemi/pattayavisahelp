# _research/

Raw research drops from Perplexity, Codex, and other AI tools. Source of truth for site content.

**Not deployed to the live site** — gitignored from public output (folder name starts with `_`, Cloudflare Pages doesn't serve underscore-prefixed paths by default, but we also exclude via build settings).

See `INDEX.md` for the running tracker of what's saved and what's built.
