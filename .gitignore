# Dependencies
node_modules/
.pnp/
.pnp.js

# Build outputs
dist/
.cache/
.parcel-cache/

# Environment / secrets
.env
.env.local
.env.*.local
*.key
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Wrangler / Cloudflare
.wrangler/
.dev.vars
.mf/

# Local-only
*.local
.history/

# Generated audits (local runs)
_audit-*.json
_lighthouse-reports/
_policy-radar.json

# Note: _research/ stays in the repo (committed) — it's the source of truth.
# It's just not served publicly because Cloudflare Pages skips underscore-prefixed paths.
.tmp.driveupload/
*.tmp.driveupload

.tmp.driveupload/
*.tmp.driveupload
